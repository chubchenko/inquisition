<div class="">
  <span class="sub-subtitle">
    <a href="#brakeman">
      Brakeman
    </a>
  </span>

  <ul class="mt--5">
    <% tally do |type, count| %>
      <li>
        <%= "#{type}: #{count}" %>
      </li>
    <% end %>
  </ul>

  <span>
    <%= "There are #{count} warnings and #{high.count} of them are high. The full report can be found here." %>
  </span>
</div>
