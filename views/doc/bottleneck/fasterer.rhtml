<li>
  <%= "there are #{collection.count} recommendations to make the application faster (the full report can be found here). Where:" %>
  <ul type="circle">
    <% collection.group do |offense, count| %>
      <li>
        <%= "#{count} of them related to the <a href=#{offense[:link]}>#{offense[:title]}</a>;" %>
      </li>
    <% end %>
  </ul>
</li>
